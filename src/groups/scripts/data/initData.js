import { setLoadFunction } from "../../../framework/state/DataInit.js";

const loadFunction = (event) => {
  /**
   * TODO
   * -Update API request state
   */
  console.log("Updating request state");
};

setLoadFunction(loadFunction);
